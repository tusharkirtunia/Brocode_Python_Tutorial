
#name = input ("Enter your full name: ")
#phone_number = input("Enter a phone number: ")

# result = len(name)
# result = name.find(" ")           #here it is determined to find the 1st occurrence
                                    #for eg: in the output section : the input will be : Tushar Kirtunia ----> ans: 6

# result = name.rfind("u")          #here it is determined to find the last occurrence

# name = name.capitalize()          #this function capitalizes the 1st letter

# name = name.upper()               #to make all the characters in upper case

# name = name.lower()               #to make all the characters in lower case

# result = name.isdigit()           #this func. returns in boolean and checks the presence of only digits
                                    #if output has both digits and letters : it will return false, for true u need only
                                    #digits in the input of the output portion

# result = name.isalpha()           #this func. returns in boolean and checks the presence of only alphabetic characters

# result = phone_number.count("7")  #The .count() method in Python is used to count the number of occurrences of a
                                    # specific element in a list, tuple, or a substring in a string.



#print(result)